# TowFish Configuration and Parameters

## Configuration Files

- List important configuration files for TowFish (with paths).
- For each file, describe its purpose (e.g., sensor settings, logging configuration).

## Key Parameters

- Summarize critical parameters (e.g., sampling rates, filter settings, thresholds).

## Platform-Specific Overrides

- Describe any configuration that depends on the tow vessel or mission type.
