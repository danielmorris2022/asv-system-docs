# WAMV-2 Configuration and Parameters

## Configuration Files

- List important configuration files for WAMV-2 (with paths).
- For each file, describe its purpose (e.g., thruster mapping, sensor offsets, depth/weather configuration).

## Key Parameters

- Summarize critical parameters (e.g., PID gains, safety limits, thresholds for depth and weather data).
- Note how these parameters were chosen (tuning process, field experience).

## Platform-Specific Overrides

- Describe any configuration that differs from other platforms (e.g., WAMV-1), and why.
