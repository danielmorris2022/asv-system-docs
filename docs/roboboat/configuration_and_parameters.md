# RoboBoat Configuration and Parameters

## Configuration Files

- List important configuration files for RoboBoat (with paths).
- For each file, describe its purpose (e.g., thruster mapping, sensor offsets).

## Key Parameters

- Summarize critical parameters (e.g., PID gains, safety limits, thresholds).
- Note how these parameters were chosen (tuning process, competition requirements).

## Platform-Specific Overrides

- Describe any configuration that differs from other platforms, and why.
